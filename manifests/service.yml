apiVersion: v1
kind: Service
metadata:
    name: "kubernetescluster-5759"
    labels:
        app: "kubernetescluster-5759"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubernetescluster-5759"